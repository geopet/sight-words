#!/usr/bin/env ruby

lib_dir = File.expand_path("../../lib", __FILE__)
$LOAD_PATH.unshift lib_dir unless $LOAD_PATH.include? lib_dir

require 'sight_words'

lesson = SightWords::Lesson.new
words_list = SightWords::WordList.new
words_list_array = %w[words_list]
p words_list_array
